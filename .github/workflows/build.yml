name: Build

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Build WebApiCore8.sln
        run: dotnet build WebApiCore8.sln --configuration Release

      - name: Build AuthService.sln
        run: dotnet build AuthService/AuthService.sln --configuration Release
